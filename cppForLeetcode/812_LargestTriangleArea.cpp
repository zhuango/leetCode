#include <vector>
using namespace std;

class Solution {
public:
    double largestTriangleArea(vector<vector<int>>& points) {
        vector<int> leftB = points[0];
        vector<int> rightB = points[0];
        vector<int> leftTop = points[0];
        vector<int> rightTop = points[0];
        for (auto &point : points)
        {
            if(point[])
        }
    }
};