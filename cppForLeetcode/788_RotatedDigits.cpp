#include <vector>
using namespace std;

class Solution {
public:
    int rotatedDigits(int N) {
        vector<bool> table= {true, true, false, false, false, false, false, false, true, false};
             
    }
};